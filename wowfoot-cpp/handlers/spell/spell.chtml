<!DOCTYPE HTML PUBLIC "-//W3C//DTD HTML 4.01//EN" "http://www.w3.org/TR/html4/strict.dtd">
<html>
<head>
	<title><%ESCAPE(mTitle);%> - Spell - WowFoot</title>
	<LINK href="static/style.css" rel="stylesheet" type="text/css">
	<META http-equiv="Content-Type" content="text/html; charset=UTF-8">
</head>
<body>

% if(!a) {
Spell <%ESCAPE(mTitle);%> does not exist.
% } else {
<p>
<span class="itemName"><%ESCAPE(a->name);%></span><br>

%// icon
<p>
% const SpellIcon* si = gSpellIcons.find(a->spellIconID);
% if(si) {
<img src="<%=getIconRaw(si->icon)%>" alt="<%=si->icon%>">
% } else {
Warning: invalid icon id (<%=a->spellIconID%>)
% }
% if(a->activeIconID) { si = gSpellIcons.find(a->activeIconID);
% if(si) {
<img src="<%=getIconRaw(si->icon)%>" alt="<%=si->icon%>">
% } else {
Warning: invalid icon id (<%=a->activeIconID%>)
% }
% }
</p>

% if(a->description) {
<span class="itemDescription"><%=a->description%></span><br>
% }
</p>
<p>Rank: <%=a->rank%></p>
<p>Tooltip: <%=a->toolTip%></p>

% returnCode = tabsChtml::run(stream);

% }	//if(a)

</body>
</html>
