<%/*
static string questArea(quest) {
	z = row.zoneOrSort
	if(!z)
		return "";
	a = gAreaTable.find(z)
	if(!a)
		return "";
	return "<a href=\"area="+z+"\">"+a.name+"</a>";
}*/
%>

<p>Search results for: <%ESCAPE(urlPart);%></p>

% returnCode = tabsChtml::run(stream);
