
<p>Zone: <%ESCAPE(mTitle);%></p>

% if(mAT) {
% returnCode = mSpawnPointsChtml.run(stream);
% if(returnCode == 200) {
% returnCode = tabsChtml::run(stream);
% }
% } else {
<%= mId %> not found.
% return 404;
% }
